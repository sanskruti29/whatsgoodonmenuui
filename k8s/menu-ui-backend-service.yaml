apiVersion: v1
kind: Service
metadata:
  labels:
    app: menu-ui
  name: menu-ui-backend
  namespace: default
spec:
  externalTrafficPolicy: Cluster
  ports:
    - name: httpuiport
      port: 8080
      protocol: TCP
      targetPort: 8080
    - name: httpsuiport
      port: 443
      protocol: TCP
      targetPort: 8080
  selector:
    app: menu-ui
    tier: web
  sessionAffinity: None
  type: NodePort
